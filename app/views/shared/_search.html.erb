<%= search_form_for q, url: url do |f| %>
  <label class='input input-bordered flex items-center gap-2 mt-6 mb-6  sm:mx-32'>
    <%= f.text_field :title_or_body_or_spots_store_name_cont, class:'grow', placeholder:'おすすめスポットを探す' %>
    <button type='submit' class='flex items-center gap-2 bg-transparent border-none cursor-pointer'>
      <i class="fa-solid fa-magnifying-glass"></i>
    </button>
  </label>
<% end %>